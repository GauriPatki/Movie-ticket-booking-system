from tkinter import

class login (Tk):

def

init(self):

super()._init_()

self.geometry("700x500") self.resizable(False, False)

def Label(self):

self.background Image PhotoImage(file="background_img.png")

self.backGroundImageLabel-Label (self, image=self.backGround Image) self.backGround ImageLabel.place(x=0,y=0)

name

main ":

Login=login()

Login.Label()

Login.mainloop()
